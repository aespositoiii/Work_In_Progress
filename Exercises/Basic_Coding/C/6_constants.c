#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
    
    const int A = 10;  // setting a variable as a constant prevents it from being modified.  Convention uses all caps for constants. 

    printf("a = %d\n", a);



    return 0;
}
#include <stdio.h>
#include <stdlib.h>

int main(){
    
    // Primitives
    int age = 40;
    double gpa = 3.6;
    char grade = 'A';       // Individual String objects get single quotes
    
    char phrase[] = "Short Pants";  // Arrays of char (strings) use double quotes

    return 0;
}
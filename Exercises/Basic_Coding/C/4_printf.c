#include <stdio.h>
#include <stdlib.h>

int main(){
    
    printf("Hello Whirl\n");
    printf("\"\n");
    printf("%d", 500); // %c is for chars, %s is for strings, %d is for integers, %f is for floats

    return 0;
}